services:
  app:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    image: iflab-backend
    restart: unless-stopped
    tty: true
    ports:
      - "8000:8000"
    environment:
      APP_NAME: "IFG Lab Manager"
      APP_ENV: local
      APP_KEY: ${APP_KEY:-base64:Y6ApjMGzoygQtDBzEwt3ffjadUFpNBX74EhMLdKcUJs=}
      APP_DEBUG: "true"
      APP_URL: "http://localhost"
      APP_TIMEZONE: ${APP_TIMEZONE:-America/Sao_Paulo}
      FRONTEND_URL: ${FRONTEND_URL:-http://localhost}
      DB_CONNECTION: pgsql
      DB_HOST: db
      DB_PORT: 5432
      DB_DATABASE: app
      DB_USERNAME: user
      DB_PASSWORD: password
      REDIS_HOST: redis
      REDIS_PASSWORD: null
      REDIS_PORT: 6379
      REDIS_PREFIX: iflab_database_
      QUEUE_CONNECTION: redis
      OCTANE_SERVER: roadrunner
    volumes:
      - ./backend:/var/www/html
      - ./agent:/var/www/agent
      - ./docker/php/php.ini:/usr/local/etc/php/conf.d/99-custom.ini
    command: sh -c "php artisan storage:link 2>/dev/null || true && exec php artisan octane:start --server=roadrunner --host=0.0.0.0 --port=8000"
    depends_on:
      - db
      - redis
    networks:
      - iflab-net

  scheduler:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    image: iflab-backend
    restart: unless-stopped
    environment:
      APP_NAME: "IFG Lab Manager"
      APP_ENV: local
      APP_KEY: ${APP_KEY:-base64:Y6ApjMGzoygQtDBzEwt3ffjadUFpNBX74EhMLdKcUJs=}
      APP_DEBUG: "true"
      APP_URL: "http://localhost"
      APP_TIMEZONE: ${APP_TIMEZONE:-America/Sao_Paulo}
      FRONTEND_URL: ${FRONTEND_URL:-http://localhost}
      DB_CONNECTION: pgsql
      DB_HOST: db
      DB_PORT: 5432
      DB_DATABASE: app
      DB_USERNAME: user
      DB_PASSWORD: password
      REDIS_HOST: redis
      REDIS_PASSWORD: null
      REDIS_PORT: 6379
      REDIS_PREFIX: iflab_database_
      QUEUE_CONNECTION: redis
    volumes:
      - ./backend:/var/www/html
      - ./docker/php/php.ini:/usr/local/etc/php/conf.d/99-custom.ini
    command: php artisan schedule:work
    depends_on:
      - db
      - redis
    networks:
      - iflab-net

  queue-worker:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    image: iflab-backend
    restart: unless-stopped
    environment:
      APP_NAME: "IFG Lab Manager"
      APP_ENV: local
      APP_KEY: ${APP_KEY:-base64:Y6ApjMGzoygQtDBzEwt3ffjadUFpNBX74EhMLdKcUJs=}
      APP_DEBUG: "true"
      APP_URL: "http://localhost"
      APP_TIMEZONE: ${APP_TIMEZONE:-America/Sao_Paulo}
      FRONTEND_URL: ${FRONTEND_URL:-http://localhost}
      DB_CONNECTION: pgsql
      DB_HOST: db
      DB_PORT: 5432
      DB_DATABASE: app
      DB_USERNAME: user
      DB_PASSWORD: password
      REDIS_HOST: redis
      REDIS_PASSWORD: null
      REDIS_PORT: 6379
      REDIS_PREFIX: iflab_database_
      QUEUE_CONNECTION: redis
    volumes:
      - ./backend:/var/www/html
      - ./docker/php/php.ini:/usr/local/etc/php/conf.d/99-custom.ini
    command: php artisan queue:work redis --sleep=3 --tries=3 --max-time=3600 --verbose
    depends_on:
      - db
      - redis
    networks:
      - iflab-net

  nginx:
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
      # In dev, we might proxy to frontend dev server, but for "prod-like" setup:
      - ./frontend/dist:/var/www/frontend
      # Backend storage (uploaded files: lab wallpapers, etc.) - mount the actual dir so /storage/ resolves
      - ./backend/storage/app/public:/var/www/backend-storage:ro
    networks:
      - iflab-net

  # For development convenience, a node service to run build
  node:
    image: node:20
    working_dir: /app
    volumes:
      - ./frontend:/app
    command: npm run build -- --watch
    networks:
      - iflab-net

  db:
    image: timescale/timescaledb:latest-pg15
    restart: unless-stopped
    command: postgres -c shared_preload_libraries=timescaledb
    environment:
      POSTGRES_DB: app
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    volumes:
      - db-data:/var/lib/postgresql/data
    networks:
      - iflab-net

  redis:
    image: redis:alpine
    restart: unless-stopped
    volumes:
      - redis-data:/data
    networks:
      - iflab-net

  reverb:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    image: iflab-backend
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      APP_NAME: "IFG Lab Manager"
      APP_ENV: local
      APP_KEY: ${APP_KEY:-base64:Y6ApjMGzoygQtDBzEwt3ffjadUFpNBX74EhMLdKcUJs=}
      APP_DEBUG: "true"
      APP_URL: "http://localhost"
      APP_TIMEZONE: ${APP_TIMEZONE:-America/Sao_Paulo}
      FRONTEND_URL: ${FRONTEND_URL:-http://localhost}
      DB_CONNECTION: pgsql
      DB_HOST: db
      DB_PORT: 5432
      DB_DATABASE: app
      DB_USERNAME: user
      DB_PASSWORD: password
      REDIS_HOST: redis
      REDIS_PASSWORD: null
      REDIS_PORT: 6379
      REVERB_APP_ID: ${REVERB_APP_ID:-iflab-app}
      REVERB_APP_KEY: ${REVERB_APP_KEY:-iflab-key}
      REVERB_APP_SECRET: ${REVERB_APP_SECRET:-iflab-secret}
      REVERB_HOST: ${REVERB_HOST:-localhost}
      REVERB_PORT: ${REVERB_PORT:-8080}
      REVERB_SCHEME: ${REVERB_SCHEME:-http}
      BROADCAST_CONNECTION: reverb
    volumes:
      - ./backend:/var/www/html
      - ./docker/php/php.ini:/usr/local/etc/php/conf.d/99-custom.ini
    command: php artisan reverb:start --host=0.0.0.0 --port=8080
    depends_on:
      - db
      - redis
    networks:
      - iflab-net

networks:
  iflab-net:
    driver: bridge

volumes:
  db-data:
  redis-data:
